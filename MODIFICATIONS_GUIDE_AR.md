# 📝 دليل التعديل السريع - Clinck

هذا الملف يشرح كيفية تعديل الأشياء الشائعة بسهولة في نظام Clinck

---

## 📦 الاشتراكات الشهرية (Monthly Subscriptions)

### موقع البيانات الحالية

```
components/TreatmentsSection.tsx
```

### كيفية التعديل

#### 1️⃣ **إضافة اشتراك جديد**

اذهب إلى `components/TreatmentsSection.tsx` وأضف الخدمة الجديدة في الـ array:

```typescript
const treatments = [
  {
    id: 5,                           // رقم فريد
    nameAr: "اسم الخدمة بالعربية",
    nameEn: "Service Name in English",
    descriptionAr: "وصف الخدمة بالعربية",
    descriptionEn: "Service description in English",
    price: 500,                      // السعر بالجنيه
    icon: "🦷"                       // الرمز
  }
];
```

#### 2️⃣ **تعديل سعر خدمة موجودة**

ابحث عن الخدمة بـ `id` وغيّر قيمة `price`:

```typescript
{
  id: 3,
  nameAr: "تنظيف الأسنان",
  nameEn: "Teeth Cleaning",
  price: 150  // ✏️ غير هنا
}
```

#### 3️⃣ **حذف خدمة**

ابحث عن الخدمة بـ `id` وـ delete الكائن كله من الـ array:

```typescript
// قبل
const treatments = [
  { id: 1, ... },
  { id: 2, ... },  // ❌ احذفها
  { id: 3, ... }
];

// بعد
const treatments = [
  { id: 1, ... },
  { id: 3, ... }
];
```

#### 4️⃣ **تغيير اسم الخدمة**

```typescript
{
  id: 1,
  nameAr: "اسم جديد بالعربية",      // ✏️ غير هنا
  nameEn: "New English Name",        // ✏️ وهنا
  ...
}
```

---

## 🎨 الألوان والتصميم

### تغيير الألوان الأساسية

```
tailwind.config.ts
```

```typescript
colors: {
  primary: '#your-color',    // اللون الأساسي
  secondary: '#your-color',  // اللون الثانوي
  accent: '#your-color'      // لون التركيز
}
```

---

## 🌍 اللغات والترجمات

### إضافة كلمة جديدة

#### في الملف العربي:

```json
// messages/ar.json
{
  "new": {
    "term": "الترجمة العربية"
  }
}
```

#### في الملف الإنجليزي:

```json
// messages/en.json
{
  "new": {
    "term": "English translation"
  }
}
```

#### استخدامها في المكون:

```typescript
const { t } = useTranslation();
<p>{t('new.term')}</p>
```

---

## 📱 الخدمات المتاحة

### الخدمات الحالية

| الكود | الخدمة | السعر |
|------|--------|------|
| teeth-cleaning | تنظيف الأسنان | 150 جنيه |
| whitening | تبييض الأسنان | 500 جنيه |
| filling | حشو الأسنان | 300 جنيه |
| root-canal | علاج الجذور | 800 جنيه |
| extraction | خلع الأسنان | 400 جنيه |

### إضافة خدمة جديدة في نموذج الحجز

#### 1. في الفرونت اند:

اذهب إلى `components/Booking/ServiceSelector.tsx` وأضف الخدمة:

```typescript
const services = [
  {
    id: 'new-service',
    nameAr: 'اسم الخدمة',
    nameEn: 'Service Name',
    icon: '🦷'
  }
];
```

#### 2. في الباك اند:

اذهب إلى `app/Http/Controllers/Api/AppointmentController.php` وأضف التحقق:

```php
'service' => 'required|in:teeth-cleaning,whitening,filling,root-canal,extraction,new-service'
```

---

## 🔐 بيانات الاتصال والهاتف

### موقع بيانات العيادة

```
محفوظة في:
- lib/api.ts (API_BASE_URL)
- .env.local (NEXT_PUBLIC_CLINIC_PHONE)
```

### تعديل رقم WhatsApp

في ملف `.env.local`:

```env
NEXT_PUBLIC_CLINIC_PHONE=+201110215455
```

غيّره إلى رقمك الجديد.

---

## 🗓️ أوقات العمل والمواعيد

### تغيير أوقات الحجز المتاحة

اذهب إلى `components/Booking/DateTimeSelector.tsx`:

```typescript
const timeSlots = [
  '09:00 AM',
  '09:30 AM',
  '10:00 AM',
  '10:30 AM',
  // أضف أو احذف أوقات هنا
];
```

### منع حجز في أيام معينة

في `DateTimeSelector.tsx`:

```typescript
const disabledDates = [
  new Date(2026, 1, 20),  // 20 فبراير 2026
  new Date(2026, 1, 21),  // 21 فبراير 2026
];

// في الـ DatePicker:
disabled={(date) => 
  disabledDates.some(d => isSameDay(d, date))
}
```

---

## 💰 الأسعار والإيرادات

### حساب العائد المتوقع

في `components/Admin/Dashboard.tsx`:

```typescript
const PRICES = {
  'teeth-cleaning': 150,
  'whitening': 500,
  'filling': 300,
  'root-canal': 800,
  'extraction': 400
};

// يتم حساب الإيراد تلقائياً من الأسعار
```

لتغيير سعر، غيّر القيمة في هذا الـ object.

---

## 🔧 API و الاتصال بالباك اند

### إضافة دالة API جديدة

في `lib/api.ts`:

```typescript
const api = {
  // ...existing endpoints
  
  newFeature: async (data: any) => {
    const response = await fetch(`${API_BASE_URL}/new-endpoint`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-Admin-Token': process.env.NEXT_PUBLIC_ADMIN_TOKEN || ''
      },
      body: JSON.stringify(data)
    });
    
    if (!response.ok) throw new Error('Failed to create');
    return response.json();
  }
};
```

### استخدامها في المكون:

```typescript
const response = await api.newFeature(data);
```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: الحجوزات لا تظهر

**الحل:**
```bash
# فتح Developer Tools (F12)
# 1. اذهب إلى Network tab
# 2. تحقق من أن طلب `/api/v1/appointments` يعود بـ 200
# 3. إذا كان هناك خطأ، اطبع الخطأ في Console
```

### المشكلة: الترجمة لا تعمل

**الحل:**
```typescript
// تأكد من أن المفتاح موجود في messages/ar.json و messages/en.json
// أضفت المفتاح الجديد؟ امسح الكاش وأعد التحميل
```

### المشكلة: الأوقات المحجوزة لا تختفي

**الحل:**
```bash
# في الباك اند، تأكد من:
php artisan migrate
# إعادة تشغيل الخادم
php artisan serve
```

---

## 📊 الإحصائيات

### عرض إحصائيات المرضى

في `components/Admin/Dashboard.tsx` يتم حساب:
- عدد المواعيد الكلي
- عدد المواعيد المؤكدة
- عدد المواعيد المعلقة
- الإيراد المتوقع

لتغيير طريقة الحساب، عدّل دالة `fetchAppointments()`.

---

## 🎯 الخطوات التالية

### بعد إجراء تعديل:

1. **اختبر التغيير محلياً:**
   ```bash
   npm run dev
   ```

2. **تحقق في المتصفح:**
   - http://localhost:3000

3. **إذا كان هناك أخطاء:**
   - اضغط F12 لفتح Developer Tools
   - انظر إلى Console tab للأخطاء

4. **عندما تنتهي:**
   ```bash
   git add .
   git commit -m "تعديل: وصف التغيير"
   git push
   ```

---

## 🔗 روابط مهمة

- [README العربي](README_AR.md) - الوثائق الكاملة بالعربية
- [README الإنجليزي](README_EN.md) - Complete documentation in English

---

**آخر تحديث**: 17 فبراير 2026
